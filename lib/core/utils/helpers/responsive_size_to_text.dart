/*
import 'package:birthday_card_app/core/utils/helpers/get_scale_factor_helper.dart';
import 'package:flutter/material.dart';

double responsiveSizeToText(
    {required double fontSize, required BuildContext context}) {
  double scaleFactor = getScaleFactorHelper(context: context);
  double newFontSize = fontSize * scaleFactor;
  double lowerLimit = fontSize * 0.8;
  double upperLimit = fontSize * 1.2;
  return newFontSize.clamp(
    lowerLimit,
    upperLimit,
  );
}
*/
